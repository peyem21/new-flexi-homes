import Nav from './Nav';
import HeroSection from './HeroSection';

const NavSec = () => {
    return (
        <div className="office z-50">
            <Nav />
            <HeroSection />
        </div>
    );
};

export default NavSec;
